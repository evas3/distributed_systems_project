# distributed_systems_project
Project for HY course distributed systems

## Installing dependencies ann starting the application
You can install the dependencies required by the project with poetry:
```
poetry install
```

After you have installed the dependencies, you can start the application with the following command:

```
poetry run invoke start
```